query FeedbackReactionCounts($ids: [ID!]!) {
  nodes(ids: $ids) {
    ... on Discussion {
      id
      locked
      updatedAt
      reactionGroups {
        content
        users {
          totalCount
        }
      }
    }
  }
}
