mutation FeedbackPollVote($id: ID!) {
  addDiscussionPollVote(input: {pollOptionId: $id}) {
    pollOption {
      poll {
        options(first: 100) {
          nodes { id totalVoteCount viewerHasVoted }
        }
      }
    }
  }
}
