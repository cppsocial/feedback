query FeedbackDiscussionReactions($ids: [ID!]!) {
  nodes(ids: $ids) {
    ... on Discussion {
      id
      locked
      updatedAt
      reactionGroups {
        content
        reactors { totalCount }
      }
    }
  }
}
