query FeedbackDiscussionReactions($ids: [ID!]!, $includeReactions: Boolean!) {
  nodes(ids: $ids) {
    ... on Discussion {
      id
      locked
      updatedAt
      upvoteCount
      reactionGroups @include(if: $includeReactions) {
        content
        users { totalCount nodes { id } }
      }
    }
  }
}
