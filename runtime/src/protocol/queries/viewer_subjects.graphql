query FeedbackViewerSubjects($ids: [ID!]!) {
  nodes(ids: $ids) {
    id
    ... on Reactable {
      reactionGroups { content viewerHasReacted }
    }
    ... on DiscussionPollOption { viewerHasVoted }
  }
}
