query FeedbackDiscussionReactions($ids: [ID!]!) {
  nodes(ids: $ids) {
    ... on Discussion {
      id
      locked
      updatedAt
      upvoteCount
      reactionGroups {
        content
        users { totalCount nodes { id } }
      }
    }
  }
}